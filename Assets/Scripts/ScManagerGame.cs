using UnityEngine;

public class ScManagerGame : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //GetComponent<EnemyFactory>()
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
